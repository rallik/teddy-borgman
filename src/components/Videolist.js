import React from 'react'
import { Video } from './index'

const Videolist = (props) => {
    console.log(props)
    // const page = location.pathname.includes("work")

    return (
        <section className='section video-list'>
            vid 1 vid 2
        </section>
    )
}

export default Videolist
